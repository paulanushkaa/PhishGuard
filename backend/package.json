{
  "name": "phishguard-webrisk-backend",
  "version": "1.0.0",
  "description": "Minimal backend that lets the PhishGuard Chrome extension validate URLs against Google Web Risk without ever exposing the Google API key to the extension.",
  "private": true,
  "type": "commonjs",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "express-rate-limit": "^7.4.0",
    "helmet": "^7.1.0"
  }
}
